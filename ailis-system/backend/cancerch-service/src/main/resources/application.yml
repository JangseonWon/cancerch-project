spring:
  application:
    name: cancerch-service

  r2dbc:
    url: r2dbc:postgresql://localhost:5432/ailis
    username: ailis
    password: ailis1234
    pool:
      initial-size: 10
      max-size: 50
      max-idle-time: 30m

  flyway:
    enabled: true
    url: jdbc:postgresql://localhost:5432/ailis
    user: ailis
    password: ailis1234
    locations: classpath:db/migration
    baseline-on-migrate: true
    schemas: public

  jackson:
    serialization:
      write-dates-as-timestamps: false
    deserialization:
      fail-on-unknown-properties: false

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  metrics:
    export:
      prometheus:
        enabled: true

logging:
  level:
    root: INFO
    com.idrsys.ailis: DEBUG
    org.springframework.r2dbc: DEBUG
