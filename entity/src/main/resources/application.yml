spring:
  datasource:
    url: jdbc:postgresql://gemini:5432/lims
    username: avoid
    password: avoid1004
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
server:
  port: 0