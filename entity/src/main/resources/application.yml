spring:
  datasource:
    url: jdbc:postgresql://aries:5432/lims
    username: avoid
    password: avoid1004
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
server:
  port: 0